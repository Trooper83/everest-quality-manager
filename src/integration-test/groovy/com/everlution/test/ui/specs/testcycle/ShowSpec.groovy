package com.everlution.test.ui.specs.testcycle

import geb.spock.GebSpec
import grails.testing.mixin.integration.Integration

@Integration
class ShowSpec extends GebSpec {

    void "back to release plan link goes to release plan"() {
        expect:
        assert false
    }

    void "correct fields are displayed"() {
        expect:
        assert false
    }

    void "create delete edit buttons not displayed for Read Only user"() {
        expect:
        assert false
    }

    void "create delete edit buttons displayed for authorized users"() {
        expect:
        assert false
    }

    void "test cycle not deleted if alert is canceled"() {
        expect:
        assert false
    }

    void "updated message displays after updating test cycle"() {
        expect:
        assert false
    }

    void "verify table headers order"() {
        expect:
        assert false
    }

    void "test iteration table id link opens show test iteration"() {
        expect:
        assert false
    }
}
