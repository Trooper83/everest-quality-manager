package com.manager.quality.everest

class SearchResult {

    SearchResult(List results, int count) {
        this.results = results
        this.count = count
    }

    List results
    int count
}
